# Домашний воркер: RTSP, детекция движения, YOLO. Ставится на машине рядом с камерами.
# Ставить на сервер не нужно — он этих зависимостей не использует.

ultralytics>=8.1
opencv-python>=4.9
httpx>=0.27
PyYAML>=6.0
