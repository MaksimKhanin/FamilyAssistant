"""Модуль «Подход» — фоново ведёт короткий профиль о том, что работает в
общении с этим человеком, и держит его под рукой у модели.

Своих таблиц у модуля нет: заметки и итоги разговоров живут на двух личных
досках, которые модуль заводит себе так же лениво, как memory заводит
«Память ассистента» (см. app/modules/memory/knowledge.py —
approach_notes_board/approach_summaries_board). Решение объяснено в
docs/adr/0011-pravilo-lozhitsya-tuda-gde-deystvuet.md: новая таблица здесь
потребовала бы своего экрана — и он получился бы доской, только хуже.

Разбор запускает планировщик (app/scheduler.py, run_relationship_reviews),
а не событие в теле чата: живой разговор не должен ждать лишний вызов модели.
"""
from app.core.module import Module
from app.modules.relationship import tools  # noqa: F401  (регистрирует инструмент)

module = Module(
    name="relationship",
    title="Подход",
    description=("Раз в несколько сообщений сам разбирает разговор и обновляет "
                 "короткие выводы о том, что работает в общении"),
    memo_hint=("Что ассистенту стоит держать в уме об общении с вами — вашими "
               "словами. Свою выжимку модуль «Подход» ведёт отдельной строкой "
               "ниже и написанное здесь руками не трогает."),
    per_user=True,
)
