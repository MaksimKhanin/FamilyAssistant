"""Модуль «Интернет» — общий поиск наружу для ответов на вопросы.

Дверь наружу одна и уже существует — `app/core/websearch.py`; до сих пор ею
пользовалось только питание (`lookup_product`). Этот модуль отдаёт её модели
как обычный инструмент `search_web`: вопрос про внешний мир и свежие факты →
поиск → связный ответ с названными источниками.

Экрана у модуля нет — только инструмент и тумблер в матрице модулей: админ
решает, кому ассистент может ходить наружу. Без настроенного
`WEB_SEARCH_PROVIDER` инструмент в схему модели не попадает вовсе
(`ToolSpec.available`), и модуль ведёт себя как пустой.
"""
from app.core.module import Module
from app.modules.web import tools  # noqa: F401  (регистрирует инструмент)

module = Module(
    name="web",
    title="Интернет",
    description="Ассистент ищет ответы в интернете и называет источники",
    per_user=True,
)
